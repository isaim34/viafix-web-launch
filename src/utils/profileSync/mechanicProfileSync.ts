
// Removed mechanic profile sync - all data comes from Supabase
export const syncMechanicAvatar = () => {
  // No longer needed - data comes from database
};

export const syncMechanicName = () => {
  // No longer needed - data comes from database
};

export const syncMechanicProfileData = () => {
  // No longer needed - data comes from database
};
